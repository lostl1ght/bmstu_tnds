#ifndef INPT_OUPT_H
#define INPT_OUPT_H

#include <stdio.h>
#include "number.h"

int input_str(char str[]);
void output_check(const number_t *num);
void output_num(const number_t *num);

#endif